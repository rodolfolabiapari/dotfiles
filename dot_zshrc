# -*- mode:bash; tab-with=2; indent-tabs-mode=0; -*-
# vim:ft=bash

# Carreaga em ordem numérica

if [ -d "${ZDOTDIR:-$HOME}/.zshrc.d/" ]; then
  for config_file in "${ZDOTDIR:-$HOME}"/.zshrc.d/*; do
    [ -f "$config_file" ] && source "$config_file"
  done
fi

