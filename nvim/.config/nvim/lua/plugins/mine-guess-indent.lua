-- guess-indent.nvim: detecta automaticamente a indentação (tabs vs espaços,
-- largura) do arquivo atual com base no conteúdo, evitando precisar configurar
-- manualmente a cada projeto.

return {
  { "NMAC427/guess-indent.nvim", opts = {} },
}
