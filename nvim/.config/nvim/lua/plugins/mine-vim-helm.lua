-- vim-helm: syntax highlighting e identação para templates Helm (.tpl, .yaml)
-- — útil para manifests K8s com valores {{ .Values... }}.

return {
  {
    "towolf/vim-helm",
    lazy = false,
  },
}
